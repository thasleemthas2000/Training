server:
  port: 8040

spring:
  application:
    name: driver-service-client
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka